# The Google Client ID is NOT a secret — it's safe to commit.
# Instead of using this env var, just edit src/config.js directly
# and replace YOUR_CLIENT_ID_HERE with your actual Client ID.
#
# This file is here only if you prefer the env var approach.
VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE.apps.googleusercontent.com
